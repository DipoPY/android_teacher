---
id: kotlin.scope-functions.q06
type: true-false
---
`with` — функция-расширение, поэтому её можно вызвать через `?.`.

## Answer
false

## Explanation
with — обычная функция, объект передаётся аргументом: `with(obj) { … }`. Безопасный вызов через `?.` к ней неприменим.
