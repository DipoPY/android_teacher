---
id: kotlin.scope-functions.q10
type: true-false
---
`takeIf` возвращает объект, если предикат истинен, и null в противном случае.

## Answer
true

## Explanation
`x.takeIf { predicate }` возвращает x, если предикат true, иначе null. takeUnless работает наоборот.
